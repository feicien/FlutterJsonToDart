<idea-plugin>
    <id>cn.dxy.app.DxyJsonToDart</id>
    <name>FlutterJsonToDart</name>
    <vendor email="chengh@dxy.cn" url="https://app.dxy.cn">DXY</vendor>

    <description><![CDATA[
The plugin can help you generate dart model needed for json_serializable from a JSON data and add json_serializable dependencies and run the <b>flutter pub run build_runner build -delete-conflicting-outputs</b> command. <br><br>
<br>
这个插件可以帮你自动把 json 数据生成带 json_serializable 注解的模型类，可以自动在 pubspec.yaml 文件添加 json_serializable 相关依赖，并且可以自动运行 <b>flutter pub run build_runner build -delete-conflicting-outputs</b> 命令，
如果你需要修改生成字段，可以通过快捷键 alt + W 或通过菜单 Build -> Run 'flutter pub run build_runner watch' 运行 watch 命令
<br>
<h4>how to use</h4>
<ol>
    <li>Launch the IDE and open plugin settings</li>
    <li>Search for "FlutterJsonToDart" and click install</li>
    <li>Open a flutter project, Right click on package -> New -> FlutterJsonToDart</li>
    <li>Input ClassName and JSON Text，and click Ok button</li>
    <li>The plugin will automatically add json_serializable related dependencies in the pubspec.yaml file</li>
    <li>The plugin will automatically generate model classes</li>
    <li>The plugin will automatically execute the command：flutter packages pub run build_runner build</li>
</ol>
    ]]>
    </description>

    <change-notes><![CDATA[
      1.0.7
       <ul>
       <li>1、make Property final</li>
       <li>3、support shortcut 'alt + W' run "flutter pub run build_runner watch --delete-conflicting-outputs" command</li>
       <li>4、support shortcut 'alt + B' run "flutter pub run build_runner build --delete-conflicting-outputs" command</li>
       </ul>
      <p>
      1.0.6
      <ul>
      <li>New: Support shortcut alt + F  or click menu Build -> Run 'flutter pub run build_runner watch'</li>
      <li>modify input json dialog ui</li>
      <li>DxyJsonToDart rename to FlutterJsonToDart</li>
      </ul>
      <p>
      1.0.5
      <ul>
      <li>New: Automatically generate dart model classes from JSON</li>
      <li>New: Automatically add json_serializable related dependencies in the pubspec.yaml file</li>
      <li>New: Automatically execute the command：flutter packages pub run build_runner build</li>
      </ul>
    ]]>
    </change-notes>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>Dart</depends>
    <depends>com.jetbrains.sh</depends>
    <depends>org.jetbrains.plugins.terminal</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="FlutterJsonToDart" displayType="BALLOON" />
    </extensions>
    <actions>
        <action id="FlutterJsonToDart" class="cn.dxy.app.dxyjsontodart.FlutterJsonToDartAction" text="FlutterJsonToDart"
                icon="AllIcons.Actions.AddFile"
                description="Generate dart bean class File from JSON">
            <add-to-group group-id="NewGroup" anchor="first"/>
        </action>

        <action id="FlutterJsonToDart.RunBuildCommand" class="cn.dxy.app.dxyjsontodart.RunBuildCommandAction"
                text="Run 'Build Runner Build' Command"
                description="Run 'flutter pub run build_runner build --delete-conflicting-outputs' command">
            <add-to-group group-id="BuildMenu" anchor="first" />
            <keyboard-shortcut keymap="$default" first-keystroke="alt b"/>
        </action>

        <action id="FlutterJsonToDart.RunWatchCommand" class="cn.dxy.app.dxyjsontodart.RunWatchCommandAction"
                text="Run 'Build Runner Watch' Command"
                description="Run 'flutter pub run build_runner watch --delete-conflicting-outputs' command">
            <add-to-group group-id="BuildMenu" anchor="after" relative-to-action="FlutterJsonToDart.RunBuildCommand"/>
            <keyboard-shortcut keymap="$default" first-keystroke="alt w"/>
        </action>

    </actions>
</idea-plugin>